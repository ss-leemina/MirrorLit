<!DOCTYPE html>
<html>

<head>
  <title>
    <%= article.title %>
  </title>
</head>

<body>
  <p>
    <%= article.press %>
  </p>
  <h1>
    <%= article.title %>
  </h1>
  <p>
    <%= new Date(article.created_at).toLocaleString('ko-KR') %>
  </p>
  <p>
    <%= article.author %>
  </p>
  <hr>
  <div>
    <%= article.content %>
      <img src="<%=image.image_url %>" alt=" 대체 사진">
  </div>
</body>

</html>